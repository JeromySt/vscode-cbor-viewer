/**
 * @fileoverview Value Types (pretty core).
 *
 * - Shared primitives used across pretty formatting and extenders.
 * - Focus on small, well-tested helpers and types.
 */
export type ValueType = 'string' | 'uint' | 'int' | 'bytes' | 'array' | 'map' | 'bool' | 'unknown' | 'binary';
